# Legacy Deployment Experiments

This folder preserves AWS ECS Express Mode and older Render deployment notes/configs. The current production direction is:

```text
Amplify frontend + Railway backend + Supabase Auth/storage
```

Keep these files as reference only unless AWS is revived later.

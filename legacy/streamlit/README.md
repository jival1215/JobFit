# Legacy Streamlit MVP

This folder preserves the original Streamlit prototype and CSV-based saved job tracker. It is not part of the active production app.

Active product code now lives in:

- `backend/` for FastAPI.
- `frontend/` for Next.js.
- `requirements-api.txt` for backend dependencies.
